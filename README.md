# time2time

## Chronos Intervention
You can run the chronos intervention to generate the intervention plots by running the following command:

```bash
cd src
python stylised_forecaster_chronos.py
```
